import { Box } from '@mui/material';
import React from 'react';

const Global = () => {
  return (
    <Box
      sx={{
        background: 'black',
        padding: '1rem',
        width: '250px',
        borderRadius: '4px',
      }}
    >
      Global
    </Box>
  );
};

export default Global;
